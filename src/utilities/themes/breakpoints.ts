import { ThemeBreakpoints } from '@/types/utilities/themes';

const breakpoints: Record<ThemeBreakpoints, number> = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
};

export default breakpoints;
